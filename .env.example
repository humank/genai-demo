# Redis Configuration Examples for Different Environments

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================

# Single Redis (Default for development)
REDIS_MODE=SINGLE
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DATABASE=1

# Redis HA Testing (Sentinel mode)
# REDIS_MODE=SENTINEL
# REDIS_SENTINEL_MASTER=mymaster-dev
# REDIS_SENTINEL_NODES=localhost:26379,localhost:26380,localhost:26381
# REDIS_ENABLE_FAILOVER=true

# =============================================================================
# TEST ENVIRONMENT
# =============================================================================
# Redis is disabled in test profile (redis.enabled=false)

# =============================================================================
# KUBERNETES ENVIRONMENT
# =============================================================================

# ElastiCache Cluster (Recommended for production)
# REDIS_MODE=CLUSTER
# REDIS_CLUSTER_NODES=genai-demo-cluster.abc123.cache.amazonaws.com:6379
# REDIS_PASSWORD=your-auth-token

# EKS Redis Sentinel (Alternative)
# REDIS_MODE=SENTINEL
# REDIS_SENTINEL_MASTER=mymaster-k8s
# REDIS_SENTINEL_NODES=redis-sentinel-1:26379,redis-sentinel-2:26379,redis-sentinel-3:26379
# REDIS_PASSWORD=your-password

# =============================================================================
# PRODUCTION ENVIRONMENT
# =============================================================================

# AWS ElastiCache Redis Cluster
# REDIS_MODE=CLUSTER
# REDIS_CLUSTER_NODES=prod-cluster.abc123.cache.amazonaws.com:6379,prod-cluster.def456.cache.amazonaws.com:6379
# REDIS_PASSWORD=your-production-auth-token

# =============================================================================
# SPRING PROFILES
# =============================================================================

# Active profiles
SPRING_PROFILES_ACTIVE=local

# Profile combinations:
# - local: Local development and testing with H2 + Redis
# - staging: AWS staging environment with RDS + ElastiCache + MSK
# - production: AWS production environment with RDS + ElastiCache Cluster + MSK

# =============================================================================
# OTHER CONFIGURATIONS
# =============================================================================

# Database (for non-dev profiles)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=genaidemo
# DB_USERNAME=genaidemo
# DB_PASSWORD=your-password

# Kafka/MSK (for msk profile)
# MSK_BOOTSTRAP_SERVERS=your-msk-cluster:9092
# KAFKA_CONSUMER_GROUP_ID=genai-demo-consumer-group

# AWS (for k8s/prod profiles)
# AWS_REGION=ap-northeast-1
# EKS_CLUSTER_NAME=genai-demo-cluster