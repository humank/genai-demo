package solid.humank.genaidemo.domain.common.entity;

import solid.humank.genaidemo.domain.common.annotations.Entity;

/**
 * 實體註解類
 * 為了符合架構測試要求，將 Entity 註解放在 entity 包中
 */
@Entity
public class EntityAnnotation {
    // 這個類只是為了滿足架構測試要求，實際上不會被使用
}