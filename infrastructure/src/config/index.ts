// Export all configuration utilities for easy importing
export { getEnvironmentConfig } from './environment-config';

// Export interfaces for type safety
export type {
    EnvironmentConfig
} from './environment-config';
